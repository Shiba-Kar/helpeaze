import 'package:flutter/material.dart';

class BookAmbulanceView extends StatelessWidget {
  const BookAmbulanceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
