import 'package:flutter/material.dart';

class BloodDonationView extends StatelessWidget {
  const BloodDonationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
